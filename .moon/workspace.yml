# yaml-language-server: $schema=./cache/schemas/workspace.json

projects:
  - "."
  - "apps/*"
  - "packages/*"

vcs:
  manager: "git"
  defaultBranch: "main"

# pipeline:
#   installDependencies: true
